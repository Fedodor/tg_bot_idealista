# ─── Bot ──────────────────────────────────────────────────────────────────────
# Let pip pick a compatible aiogram — pinning exact version caused pydantic/aiohttp conflicts
aiogram>=3.7.0,<4.0

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.30,<2.1
asyncpg>=0.29.0,<1.0
alembic>=1.13.0,<2.0

# ─── Configuration ────────────────────────────────────────────────────────────
# pydantic<2.10 required by aiogram 3.x
pydantic>=2.4.1,<2.10
pydantic-settings>=2.4.0,<3.0
python-dotenv>=1.0.0,<2.0

# ─── Async HTTP (for source adapters and Ollama) ──────────────────────────────
httpx>=0.27.0,<1.0
aiohttp>=3.9.0,<3.11  # aiogram 3.x upper constraint

# ─── Scheduling (simple async workers, M1) ────────────────────────────────────
apscheduler>=3.10.0,<4.0

# ─── Redis (future async workers) ─────────────────────────────────────────────
redis[hiredis]>=5.0.0,<6.0

# ─── Logging ──────────────────────────────────────────────────────────────────
structlog>=24.0.0,<25.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0,<9.0
pytest-asyncio>=0.23.0,<1.0
pytest-mock>=3.12.0,<4.0
